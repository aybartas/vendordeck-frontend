import { Typography } from "@mui/material";
import React from "react";

export default function CheckoutPage() {
  return (
    <Typography>Only logged in users should be able to checkout !!</Typography>
  );
}
