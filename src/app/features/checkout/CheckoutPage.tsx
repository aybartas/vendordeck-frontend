import { Typography } from "@mui/material";

export default function CheckoutPage(){
    
    return ( 
        <Typography>
            Only logged in users should be able to checkout !!
        </Typography>
    );

}